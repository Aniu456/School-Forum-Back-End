部署时只需在 .env 填入真实值

OSS_REGION=oss-cn-hangzhou
OSS_ACCESS_KEY_ID=你的AccessKeyId # 改掉 your- 开头的占位符
OSS_ACCESS_KEY_SECRET=你的AccessKeySecret
OSS_BUCKET=school-forum-files
OSS_CUSTOM_DOMAIN=https://cdn.yourdomain.com # 如果有 CDN 就填，没有就留空

服务启动时会自动检测，日志会打印 阿里云 OSS 已初始化 或 使用本地存储，一目了然。
